import { ViewStyle } from "react-native"
import styled from "styled-components/native"

export const ScreenStyles: ViewStyle = { alignItems: "center", justifyContent: "center" }

export const GearBox = styled.TouchableOpacity`
  margin-top: 40px;
  padding: 10px;
`
